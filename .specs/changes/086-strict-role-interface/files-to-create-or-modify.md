# Arquivos previstos

- `app/dashboard/page.tsx` e `app/dashboard/projects/[id]/page.tsx`.
- Componentes de projeto, perfil e revisão em `modules/`.
- Componentes compartilhados de criação e biblioteca, com seções próprias e
  vinculadas explicitamente separadas.
- Workspaces de descoberta, definição, capítulos, metodologia e mapa final.
- `modules/profile/presentation.ts` e textos legais relacionados.
- `modules/analytics/*`.
- `app/globals.css`.
- Testes de interface e regressão textual.
