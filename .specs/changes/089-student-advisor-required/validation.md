# Validação

- Testes unitários da regra de supervisão.
- Testes de contrato da interface e das quatro APIs de progressão.
- PostgreSQL 17 isolado: rascunho permitido, avanço direto negado, revisão
  pendente aceita, aprovação pelo orientador aceita e projeto de Orientador
  autônomo.
- `npm run check`, `npm run security:audit`, `npm audit --omit=dev` e
  `git diff --check`.
- Após autorização: migration remota, E2E aluno–orientador, deploy, health,
  domínio canônico e logs.
