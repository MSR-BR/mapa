# Evidências de encerramento — Change 033

- Versão: `v19082026.6`.
- A rota `/api/support` envia para `marioreis@id.uff.br` e
  `sfranca@id.uff.br`.
- O `reply-to` permanece sendo o e-mail informado no formulário.
- Validações concluídas: typecheck, lint, 61 testes e build de produção.
