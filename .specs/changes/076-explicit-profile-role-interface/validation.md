# Validação

- Teste executável do contrato de apresentação dos dois perfis.
- Testes estáticos de menu, dashboard, primeiro acesso e projeto.
- Busca de controles e textos condicionais por papel.
- `npm run check`.
- `npm run supabase:verify-advisor-student`.
- `npm run security:audit` e `npm audit --audit-level=moderate`.
- Deploy Production, HTTP 200, `/api/health` e scan de logs.
- `git diff --check` e estado final limpo/sincronizado.
